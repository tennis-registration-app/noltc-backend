name: Verify

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run lint
      - run: npm run typecheck
      - run: npm run test

      # Integration tests run against the live Supabase instance.
      # When repository secrets are available (collaborator PRs, pushes to main),
      # this exercises the full deployed-function surface end-to-end.
      # Fork PRs are skipped via the `if:` condition — GitHub passes empty strings
      # for secrets on fork PRs, so without the condition the step would fail, not skip.
      # Note: assign-court and join-waitlist happy-path tests are time-dependent —
      # they may fail if the PR gate runs outside club operating hours (6 AM – 11 PM Central).
      - name: Run integration tests against live Supabase instance
        if: ${{ secrets.SUPABASE_URL != '' }}
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: npm run test:integration
        continue-on-error: false
